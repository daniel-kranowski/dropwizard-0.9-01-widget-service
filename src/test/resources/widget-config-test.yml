usersAndPasswords:
    fred:
        username: fred
        password: password
        roles:
          - USER
    root:
        username: root
        password: root
        roles:
          - ADMIN

#widgets:

server:
    gzip:
        enabled: true
    type: simple
    applicationContextPath: /
    connector:
        type: http
        port: 8080

